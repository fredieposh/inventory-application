<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/styles.css">
        <title><%= title %></title>
    </head>
    <body>
        <%- include("partials/header.ejs") %>
        <div class="main-container">
            <h2> Delete Product </h2>
            <div class="form-container" style="flex: 8;">
                <%- include ("partials/errors.ejs") %>
                <% if (locals.isProducts) { %>
                    <h2> No products to delete</h2>
                <%} else { %>
                <form action="/delete" method="POST" class="add-form">
                    <label for="productName">
                        Product Name:
                        <input type="text" name="productName" id="productName" list="productList" required>
                        <datalist id="productList">
                            <% productNames.forEach(productName => { %>
                                <option value="<%= productName %>">
                            <% }) %>
                        </datalist>
                    </label>
                    <div class="button-container"><button type="submit" style="padding: 0.1rem 1rem;">Delete</button></div>
                </form>
                <% } %>
            </div>
        </div>
        <div class="refs-container">
            <a href="/search">Search Product</a>
            <a href="/add">Add Product</a>
            <a href="/update">Update Product</a>
            <a href="/delete">Delete Product</a>
        </div>
    </body>
</html>